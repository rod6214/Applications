<Window x:Class="sassex.Views.Principal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:sassex.Views"
        xmlns:viewModel="clr-namespace:sassex.ViewModels"
        mc:Ignorable="d"
        Title="Principal" Height="322.125" Width="530.145"
        Closing="Window_Closing">
    <Window.DataContext>
        <viewModel:PrincipalViewModel />
    </Window.DataContext>
    <Grid>
        <TabControl>
            <TabItem Header="Configuracion">
                <local:Configuracion x:Name="config"/>
            </TabItem>
            <TabItem Header="Ejecucion">
            </TabItem>
        </TabControl>
    </Grid>
</Window>
